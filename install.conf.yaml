- clean: ['~']

- link:
    ~/.Brewfile: Brewfile
    ~/.bash_profile: bash_profile
    ~/.gitconfig: gitconfig
    ~/.gitignore: gitignore
    ~/.slate: slate
    ~/.ssh/known_hosts: ssh/known_hosts
    ~/.vimrc: vimrc
